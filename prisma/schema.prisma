// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  // directUrl dihapus karena Anda menggunakan Database Standar
}

// ==========================================
// 1. ENUMS (Tipe Data Tetap)
// ==========================================

enum UserRole {
  ADMIN
  PARTNER
  CLIENT
  USHER
}

enum PackageTier {
  ESSENTIAL
  PRESTIGE
  ROYAL
  CUSTOM
}

enum RsvpStatus {
  PENDING
  ATTENDING
  DECLINED
}

// ==========================================
// 2. AUTH MODELS (NextAuth / Auth.js)
// ==========================================

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime?
  image         String?
  password      String?
  role          UserRole  @default(CLIENT)

  // RELASI B2B: Siapa WO (Partner) yang mendaftarkan klien/usher ini?
  partnerId     String?
  partner       User?     @relation("PartnerToUsers", fields: [partnerId], references: [id])
  managedUsers  User[]    @relation("PartnerToUsers")

  // IDENTITAS PARTNER (B2B)
  companyName   String?    // Misal: "Radeva Wedding Organizer"
  companyLogo   String?    // URL gambar logo Partner dari R2

  // RELASI TEMPLATE: Template custom mana yang menjadi hak milik eksklusif user ini?
  customTemplates Template[] @relation("CustomTemplates")

  checkInsPerformed Guest[] @relation("UsherCheckIns")
  updatesPerformed  Guest[] @relation("UsherUpdates")
  
  accounts      Account[]
  sessions      Session[]
  
  // Relasi ke Invitation (Satu user bisa punya banyak undangan, misal: Resepsi Jakarta & Bali)
  invitations   Invitation[]

  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

// ==========================================
// 3. BUSINESS MODELS (Tamu, Undangan & Tier)
// ==========================================

model Invitation {
  id            String   @id @default(cuid())
  slug          String   @unique // URL Undangan: evory.com/slug
  isActive      Boolean  @default(true)

  // Tier Paket yang membatasi akses klien
  packageTier   PackageTier @default(ESSENTIAL)
  
  templateId  String?
  template    Template? @relation(fields: [templateId], references: [id])
  
  // --- DATA MEMPELAI ---
  groomName     String
  groomNick     String
  groomFather   String?
  groomMother   String?
  groomImageUrl String?
  
  brideName     String
  brideNick     String
  brideFather   String?
  brideMother   String?
  brideImageUrl String?
  
  // --- DATA ACARA ---
  eventDate     DateTime
  eventTime     String   @default("08:00 WIB - Selesai")
  location      String
  mapUrl        String?

  checkInPin        String  @default("0000") // PIN untuk override/susulan
  
  // --- ASET DIGITAL & KONFIGURASI ---
  coverImageUrl String?
  musicUrl      String?
  gallery       String[] // Array URL Foto Gallery
  themeConfig   Json?    // Simpan warna/font custom disini
  
  // --- RELASI ---
  userId        String?
  user          User?    @relation(fields: [userId], references: [id])
  
  guests        Guest[]
  wishes        Wish[]

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([slug])
}

model Guest {
  id                String     @id @default(cuid())
  
  // Identitas Tamu
  name              String
  category          String?    // VIP, Keluarga, Teman Kantor
  guestCode         String     @unique // Kode unik QR Code (misal: G-12345)
  whatsapp          String?
  
  // RSVP & Kehadiran
  rsvpStatus        RsvpStatus @default(PENDING)
  pax               Int        @default(1) // Jumlah tamu yang akan datang (Konfirmasi RSVP)
  totalPaxAllocated Int        @default(1) // Jatah kuota undangan (Max Pax)
  
  // Check-In (Buku Tamu Digital)
  isCheckedIn       Boolean    @default(false)
  checkInTime       DateTime?
  checkInPin        String?    // Pin khusus jika tamu check-in mandiri
  
  checkedInById     String?    
  checkedInBy       User?      @relation("UsherCheckIns", fields: [checkedInById], references: [id])
  
  lastUpdatedById   String?    
  lastUpdatedByUser User?      @relation("UsherUpdates", fields: [lastUpdatedById], references: [id])

  // Relasi
  invitationId      String
  invitation        Invitation @relation(fields: [invitationId], references: [id], onDelete: Cascade)
  
  wishes            Wish[]

  createdAt         DateTime   @default(now())
  updatedAt         DateTime   @updatedAt
}

model Wish {
  id           String     @id @default(cuid())
  message      String     @db.Text
  senderName   String?    // Nama pengirim (jika guest tidak login)
  
  invitationId String
  invitation   Invitation @relation(fields: [invitationId], references: [id], onDelete: Cascade)
  
  guestId      String?
  guest        Guest?     @relation(fields: [guestId], references: [id])
  
  createdAt    DateTime   @default(now())
}

// ==========================================
// 4. TEMPLATE MANAGEMENT (Katalog Desain)
// ==========================================

model TemplateCategory {
  id          String     @id @default(cuid())
  name        String     // Javanese, Modern, Minimalist
  slug        String     @unique // javanese-series
  description String?
  
  templates   Template[] // Relasi One-to-Many
  
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
}

model Template {
  id          String           @id @default(cuid())
  name        String           // Jvn-01 (Kraton)
  slug        String           @unique // jvn-01 (Ini yang dipakai di URL)
  description String?          // Deskripsi singkat

  // Mengunci template ke tier tertentu
  tier        PackageTier      @default(ESSENTIAL)

  // Jika ini produk Full Custom yang Anda jual dengan harga premium
  ownerId     String?          
  owner       User?            @relation("CustomTemplates", fields: [ownerId], references: [id])

  invitations Invitation[]
  
  // Tampilan di Katalog (Frontend)
  thumbnail   String           // URL Gambar Preview
  previewUrl  String?          // Link Demo Undangan
  previewText String?          // Teks singkatan (JVN)
  bgColor     String?          // Kode warna bg (bg-stone-900)
  textColor   String?          // Kode warna text (text-gold)

  // Status
  isActive    Boolean          @default(true)
  
  // Relasi
  categoryId  String
  category    TemplateCategory @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt
}